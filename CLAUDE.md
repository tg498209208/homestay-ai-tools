# 🏡 伴山栖湖 · AI 工具库 — Claude 项目配置

> **项目代号**：伴山栖湖 · 线上参谋部
> **总指挥**：司令官（tg498209208）
> **定位**：民宿线上运营的 AI 工具中枢，覆盖营销文案、图片处理、数据分析、客户管理全链路。

---

## 📌 项目背景

**伴山栖湖** 是一间依山傍湖的精品民宿，核心卖点包括：

| 板块 | 内容 |
|------|------|
| 🍵 围炉煮茶 | 秋冬限定体验，炭火+烤物+湖景 |
| 🍞 窑烤面包 | 自有柴烧面包窑，手工制作 |
| 💒 草坪婚礼 | 山湖之间的私密小型婚礼场地 |
| 🛏️ 山景客房 | 落地窗直面山湖，全屋地暖 |
| 🌿 湖畔景观 | 湖边栈道、日落打卡点 |

**官网仓库**：`~/民宿展示/`（纯静态 HTML + 实景图片）
**本仓库**：`~/development/homestay-ai-tools/`（AI 自动化工具）

---

## 🎯 项目目标

### 短期（1-2 周）
- [x] 小红书爆款营销模板库（围炉煮茶 / 窑烤面包 / 草坪婚礼）
- [x] 自动化文案生成器基础框架（`auto_marketer.py`）
- [ ] 接入图片 → 文案的全自动管线
- [ ] 小红书多账号定时发布工具

### 中期（1-2 月）
- [ ] AI 视觉识别：上传民宿实景图 → 自动分类场景 → 匹配模板
- [ ] 多平台适配：抖音/视频号/B站 的短视频脚本自动生成
- [ ] 客服话术库：微信/私域常见问答自动回复

### 长期（3-6 月）
- [ ] 民宿 PMS 对接：房价/房态数据看板
- [ ] 竞品监控：同区域民宿价格/评价爬虫 + 分析
- [ ] 客户画像系统：订单数据 + 社交媒体互动整合分析

---

## 🏗️ 技术栈

| 层 | 技术选型 | 说明 |
|----|---------|------|
| 后端脚本 | Python 3.10+ | 文案生成、数据处理、爬虫 |
| AI 能力 | DeepSeek API（via LiteLLM） | 文案润色、图像识别、智能问答 |
| 前端（规划中） | React / 纯静态 | 运营数据看板 |
| 数据存储 | SQLite / JSON 文件 | 轻量级，后期可迁 Supabase |
| 版本控制 | Git + GitHub | 仓库：`tg498209208/homestay-ai-tools` |
| 代理环境 | LiteLLM @ localhost:4000 | DeepSeek 代理层 |

---

## 📁 目录结构

```
homestay-ai-tools/
├── CLAUDE.md                        # 本文件——项目总规划与 AI 协作指南
├── .gitignore                       # 忽略规则
│
├── Assets/
│   └── Images/                      # 民宿实景图片库（按场景分子目录）
│       ├── 围炉煮茶/
│       ├── 窑烤面包/
│       ├── 草坪婚礼/
│       ├── 客房景观/
│       └── 湖畔落日/
│
├── Docs/
│   └── Marketing/
│       └── Xiaohongshu_Templates.md # 小红书营销模板库（手动维护）
│
├── Tools/
│   └── Scripts/
│       ├── auto_marketer.py         # 自动化营销文案生成器
│       ├── image_classifier.py      # （待建）AI 图片场景分类器
│       └── scheduler.py            # （待建）多平台定时发布工具
│
└── Output/
    └── Generated/                   # 自动生成的营销文案（不纳入 Git）
```

---

## 🔄 开发工作流

### 新功能开发流程

```
需求分析 → 原型设计 → 开发实施 → 测试验证 → 司令官审核 → 上线
```

1. **需求分析**：在对话中与司令官确认需求，产出简要需求描述
2. **原型设计**：涉及 UI/UX 时提供 ASCII 线框图或文字描述
3. **开发实施**：在 `Tools/Scripts/` 下创建脚本，在 `Docs/` 下同步更新文档
4. **测试验证**：在本地运行脚本，验证输出结果
5. **司令官审核**：展示结果，等待司令官确认
6. **上线**：git add + commit + push（见下方同步规范）

### Git 同步规范（硬性要求）

**每次任务完成后，必须自动执行：**

```bash
git add .
git commit -m "特性: <中文简要描述>"
git push
```

- 提交信息使用中文，格式：`类型: 描述`（如 `特性:` / `修复:` / `优化:` / `文档:`）
- 不等待司令官提醒，主动执行

---

## 🤖 AI 协作原则

### 1. 中文优先
所有文档、注释、提交信息均使用中文。

### 2. 实战导向
不做过度工程化设计。民宿营销需要的是**快速产出、立刻可用**的内容和工具。

### 3. 民宿业务敏感度
- 文案风格：小红书/抖音风格，接地气，多 emoji，带痛点钩子
- 图片优先：民宿营销高度依赖视觉，所有工具设计以"图 → 内容"为核心链路
- 季节性：围炉煮茶（秋冬）、婚礼（春秋）、避暑（夏）需动态切换

### 4. 模块化但不强迫
- 每个脚本独立可运行，不引入不必要的依赖
- Python 优先，纯标准库能做的事不引入第三方包
- 需要 AI 能力时，走 LiteLLM 代理调 DeepSeek API

### 5. 安全红线
- API Key 不写入代码，使用环境变量或配置文件（已在 .gitignore 排除）
- 发布类工具需人工确认步骤，禁止全自动发布

---

## 📋 常用命令

```bash
# 生成营销文案
cd Tools/Scripts && python3 auto_marketer.py

# 查看项目状态
git log --oneline -10

# 运行任意 Python 脚本（走代理）
http_proxy=http://127.0.0.1:7897 python3 Tools/Scripts/xxx.py

# 调用 DeepSeek API（通过 LiteLLM 代理）
curl http://localhost:4000/health
```

---

## 🔗 关联资源

| 资源 | 地址 |
|------|------|
| 民宿官网仓库 | `~/民宿展示/` |
| 本工具库 GitHub | https://github.com/tg498209208/homestay-ai-tools |
| 环境配置文档 | `~/claude-code-setup/` |
| LiteLLM 代理 | http://localhost:4000 |
| DeepSeek API | https://api.deepseek.com |

---

> 🫡 **时刻准备着，司令官。**
